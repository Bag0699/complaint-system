spring:
  application:
    name: Complaint System
  datasource:
    url: ${URL_DB}
    username: ${USERNAME_DB}
    password: ${PASSWORD_DB}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
#  mail:
#    host: smtp-relay.brevo.com
#    port: 587
#    username: ${USERNAME_BREVO}
#    password: ${API_KEY2_BREVO}
#    properties:
#      mail:
#        smtp:
#          auth: true


#  sendgrid:
#    api-key: ${API_KEY_SENDGRID}
#    from:
#      email: ${EMAIL_SENDGRID}
#File Storage Configuration
file:
  upload-dir: ./uploads/evidence

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000



